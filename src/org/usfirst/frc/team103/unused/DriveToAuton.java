package org.usfirst.frc.team103.unused;

import org.usfirst.frc.team103.robot.RobotMap;

import edu.wpi.first.wpilibj.DriverStation;
import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class DriveToAuton extends CommandGroup {
	
	public static boolean isRightSide;
	public static String fieldState;
	
	public static final int XTONEGATIVE = -1;
	
    public DriveToAuton() {
    	/*requires(RobotMap.drive);
    	
    	fieldState = DriverStation.getInstance().getGameSpecificMessage();
    	//isRightSide or !isRightSide selected on SmartDashboard
    	if (sideSwitch || (fieldState.charAt(0) == 'R') {
    		
    		//sequentials to sideRightSwitch
    		
    	} else if () */
    		
    	
    
    }
}
